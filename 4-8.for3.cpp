#include <stdio.h>
#pragma warning(disable:4996)

/*
int main()
{
	int count;
	int tmp;
	int total = 0;
	for (count = 1; count <= 5; count++) {
		printf("%d번째 정숫값 입력 후 enter!", count);
		scanf("%d", &tmp);
		total += tmp;
	}
	printf("읽은 정수 총합은 %d이다.", total);
}
*/